# @soulverse/control-decisions

Deterministic authority, risk and policy decisions for validated SSW Action Contracts.

This package does not sign, execute, activate mandates, attest devices, call Trust Protocol or call REV.

Important: `ALLOW_CONTINUE` means only that the current policy stage permits progression to the next required control.
