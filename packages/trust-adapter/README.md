# @soulverse/trust-adapter

Typed Trust Protocol request, transport and decision-binding runtime.

A Trust PASS is action-scoped and short-lived. It is not a reusable trust badge.

The package does not implement a production endpoint, mTLS, service-key verification, REV, signing or execution.
