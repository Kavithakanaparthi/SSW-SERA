# @soulverse/rev-adapter

Typed REV runtime gate adapter and exact decision-binding verifier.

REV is the final runtime PASS/FAIL control before the Signing Gateway. A PASS is short-lived and action-scoped. Consequential PASS defaults to single-use.

This package does not implement production networking, cryptographic service-signature verification, consumption persistence, signing or execution.
